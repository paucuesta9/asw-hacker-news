<center>
  <table id="hnmain" border="0" cellpadding="0" cellspacing="0" width="85%" bgcolor="#f6f6ef">
    <tbody>
      <%= render 'header'%>
      <tr id="pagespace" title="" style="height:10px"></tr>
        <tr id="pagespace" title="Profile: wanderingp" style="height:10px"></tr>
        <tr>
          <td>
            <table border="0" cellpadding="0" cellspacing="0">
              
            </table>
            <%= form_with model: @user do |form| %>
              
              <table border="0">
                <tr class='athing'>
                  <td valign="top">user:</td>
                  <td timestamp="1618914348"><a href="/user/<%= @user.id %>" class="hnuser"><font color="#3c963c"><%= @user.username %></font></a></td>
                </tr>
                <tr>
                  <td valign="top">created:</td>
                <td><a><%= @user.created_at %></a></td>
                </tr>
                
                <% if session[:username] and session[:user_id] == @user.id %>
                    <tr>
                  <td valign="top">about:</td>
                  <td style="overflow:hidden;">
                    <%= form.text_area :about, size: "40x7"%>
                    <font size="-2"><a href="formatdoc" tabindex="-1"><font color="#afafaf">help</font></a></font>          
                  </td>
                </tr>
             
                <% else %>
                <tr>
                  <td valign="top">about:</td>
                  <td valign="top"><%= @user.about %></td>
                </tr>
                <% end %>
                
                <tr>
                </tr>
                <!--  -->
    
                  <!-- ... -->
                  <div class="col-lg-12 mt-5">
                    <% if flash[:alert] %>
                      <div class="alert alert-warning"><%= flash[:alert] %></div>
                    <% end %>
                    <div class="mx-auto mt-5" style="width: 400px">
                  <!-- ... -->
                
                <tr>
                  <td></td>
                  <td><a href="/submissions"><u>submissions</u></a></td>
                </tr>
                <tr>
                  <td></td>
                  <td><a href="/user/comments/<%=@user.id%>"><u>comments</u></a></td>
                </tr>
                <tr>
                  <td></td>
                  <td>
                    <a href="/upvoted_submissions"><u>upvoted submissions</u></a>
                  </td>
                </tr>
                <tr>
                  <td></td>
                  <td>
                    <a href="/upvoted_comments"><u>upvoted comments</u></a>
                  </td>
                </tr>
                <tr>
                  <td></td>
                  <td>
                    <%= form_with(url: search_path, method: 'get', local: true) do %>
                      <div class="form-group">
                        <%= label_tag :comment, 'Search for a comment '%>
                        <%= text_field_tag :comment, nil, placeholder: 'Eg. aaaa', class: 'form-control' %>
                      </div>
                      <%= button_tag 'Search', class: 'btn btn-success btn-block' %>
                    <% end %>
                  </td>
                </tr>
              </table>
              <% if session[:user_id] == @user.id %>
                <br><%= form.submit %>
              <% end %>
            <% end %>
            <br><br>
          </td>
        </tr>
      </table>
    </center>
  </body>
  <script type='text/javascript' src='hn.js?CZk51y0z29TYBzr1LRkl'></script>
</html>