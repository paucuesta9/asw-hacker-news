<%= form_with(model: reply) do |form| %>
  <% if reply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reply.errors.count, "error") %> prohibited this reply from being saved:</h2>

      <ul>
        <% reply.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_area :text, size: "60x7" %>
  </div>

  <%= form.hidden_field :parent_id, :value => parent.id %>
  <%= form.hidden_field :parent_type, :value => parent.class.name %>

  <div class="actions margin_top" style="margin-bottom: 20px">
    <%= form.submit %>
  </div>
<% end %>
